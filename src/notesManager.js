'use strict';

class notesManager { 
  constructor() {
    this.notesArray = []
    
  }
}

class Note {
  constructor(content) { 
    this.content = content
    this.title = content.slice(0, 21) + '...'
    notesArray.push(note)
  }
}
